// config.js - Application Configuration
const CONFIG = {
  // Set to 'true' to use IndexedDB (Mock). Set to 'false' to use the real Backend API.
  isMockMode: true,

  // Backend API URL (for Node.js or Java backend)
  // Update this to your real backend URL when it is ready.
  apiBaseUrl: 'http://localhost:8080/api', 
};

export default CONFIG;
